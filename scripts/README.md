# Shared scripts

`check-workspace.mjs` validates CODS-P1-001 package boundaries, manifest metadata, workspace globs, and installed local links. Run it through `pnpm check` from the root. Future archive, checksum, release-manifest, drift, and artifact verification scripts belong here as their backlog tasks are implemented.
