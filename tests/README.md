# Cross-package tests

This directory will hold browser, package-consumer, and release-artifact tests that cross workspace boundaries. Tests for one component should live beside that component in the core package. The initial skeleton has no runtime or component tests.
