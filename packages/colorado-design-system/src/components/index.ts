/**
 * Component exports
 *
 * This file re-exports all public components from their respective directories.
 * Components are organized as:
 *
 * src/components/<component-name>/
 *   - <component-name>.ts         (TypeScript controller for interactive components)
 *   - <component-name>.fixture.html (semantic HTML fixture)
 *   - <component-name>.scss       (component styles)
 *   - <component-name>.metadata.json (component metadata)
 *   - accessibility/
 *     - <component-name>.evidence.md (accessibility evidence)
 *
 * When adding a new component, create its directory and export its controller below:
 * export { init as initComponentName, destroy as destroyComponentName } from './component-name/component-name.js'
 */

// Placeholder: Component exports will be added here as components are implemented
// export { init as initTag, destroy as destroyTag } from './tag/tag.js'
// export { init as initSiteAlert, destroy as destroySiteAlert } from './site-alert/site-alert.js'
