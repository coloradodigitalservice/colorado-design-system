{
  "name": "colorado-design-system-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": "24.21.0"
  },
  "scripts": {
    "check:workspace": "node scripts/check-workspace.mjs --repo",
    "validate": "turbo run validate",
    "check": "pnpm check:workspace && pnpm validate"
  },
  "devDependencies": {
    "turbo": "2.10.13"
  }
}
